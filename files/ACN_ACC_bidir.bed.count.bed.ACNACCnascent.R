sink("./ACN_ACC_bidir.bed.count.bed.ACNACCnascent.Rout")
library( DESeq )
data <- read.delim("./ACN_ACC_bidir.bed.count.bed", sep="\t", header=TRUE)
countsTable <- subset(data, select=c(5,6))
rownames(countsTable) <- data$name
conds <- c("A2C", "ACC")
cds <- newCountDataSet( countsTable, conds )
cds <- estimateSizeFactors( cds )
sizeFactors(cds)
cds <- estimateDispersions( cds, method='blind', sharingMode='fit-only' )
res <- nbinomTest( cds, "A2C", "ACC" )
plotDE <- function( res ) plot(res$baseMean,res$log2FoldChange, log="x", pch=20, cex=.1, col = ifelse( res$padj < .1, "red", "black" ) )
png('./ACN_ACC_bidir.bed.count.bed.ACNACCnascent.png')
plotDE( res )
dev.off()
resSig <- res[ res$padj < .1, ]
write.table(res, file = "./ACN_ACC_bidir.bed.count.bed.ACNACCnascent.res.txt", append = FALSE, sep = "\t")
write.table(resSig, file = "./ACN_ACC_bidir.bed.count.bed.ACNACCnascent.resSig.txt", append = FALSE, sep = "\t")
write.table(resSig[ order(resSig$pval), ], file = "./ACN_ACC_bidir.bed.count.bed.ACNACCnascent.resSig_pvalue.txt", append = FALSE, sep = "\t")
sink()
